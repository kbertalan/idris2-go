1/1: Building Basics13 (Basics13.idr)
Now compiling the executable: test
printing source:

package main

import (
	"fmt"
	"math"
)

func main() {
	var x, y int = 3, 4
	var f float64 = math.Sqrt(float64(x * x + y * y))
	var z uint = uint(f)
	fmt.Println(x, y, z)
}

running generated program:

3 4 5
